struct CallbackParams
end

def resize_find_tiling_participants(current, other, direction, both_sides)
end

def percent_for_1px(con)
end

def resize_neighboring_cons(first, second, px, ppt)
end

def resize_graphical_handler(first, second, orientation, event, use_threshold)
end
