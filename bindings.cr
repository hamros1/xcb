def mode_from_name(name, pango_markup)
end

def configure_binding(bindtype, modifiers, input_code, release, border, whole_window, exclude_titlebar, command, modename, pango_markup)
end

def binding_in_current_group(bind)
end

def grab_keycode_for_binding(conn, bind, keycode)
end

def grab_all_keys(conn)
end

def regrab_all_buttons(conn)
end

def get_binding(state_filtered, is_release, input_code, input_type)
end

def get_binding_from_xcb_event(event)
end

def add_keycode_if_matches(keymap, key, data)
end

def translate_keysyms
end

def switch_mode(new_mode)
end

def reorder_binding_cmp(a, b)
end

def reorder_bindings_of_mode(mode)
end

def reorder_bindings
end

def binding_same_key(a, b)
end

def check_for_duplicate_bindings(context)
end

def binding_copy(bind)
end

def binding_free(bind)
end

def run_binding(bind, con)
end

def fill_rmlvo_from_root
end

def load_keymap
end

def bindings_get_buttons_to_grab
end
